[project]
name = "inkgrader-gateway"
version = "0.1.0"
description = "InkGrader WebSocket gateway: relays grading_updates to browsers"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]==0.116.1",
    "redis==6.4.0",
    "python-dotenv==1.1.1",
]

[tool.uv]
package = false
