[project]
name = "inkgrader-worker"
version = "0.1.0"
description = "InkGrader grading worker: polls grading_jobs and grades students"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv==1.1.1",
    "inkgrader-core",
]

[tool.uv]
package = false

[tool.uv.sources]
inkgrader-core = { workspace = true }
